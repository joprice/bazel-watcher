build --workspace_status_command="python3 tools/workplace_status.py"

# Flags used during releases
build:release --stamp
test:release --stamp
build --cxxopt='-std=c++17'
build --host_cxxopt='-std=c++17'
